// Submission button functionality (with auto submit feature) and with warning if not auto submit
// should return json file of student answers to be stored for teacher review
function Submission({ autoSub }) {
  // Must store start time and current time here. Compare with timelimit to see when to show autosubmit warning.
  // if autoSubmit then submit assessment with autosubmit warning
  // else if all questions not answered submit with confirmation
  // else submit with no warning
}
export default Submission;
